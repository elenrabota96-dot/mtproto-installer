tcp:
  routers:
    mtproxy:
      rule: "HostSNI(`vk.com`)"
      entryPoints:
        - websecure
      service: mtproxy
      tls:
        passthrough: true
  services:
    mtproxy:
      loadBalancer:
        servers:
          - address: "telemt:1234"
